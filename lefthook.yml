pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm exec oxlint --fix .
    format:
      run: pnpm exec oxfmt --write src/ tests/ scripts/

pre-push:
  commands:
    typecheck:
      run: pnpm typecheck
    test:
      run: pnpm vitest run --coverage
